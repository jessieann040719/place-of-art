
document.getElementById("demoLogin").onclick=()=>{document.getElementById("loginBox").style.display="none";document.getElementById("dash").style.display="block";renderAppts()};
document.querySelectorAll(".admin-nav button").forEach(b=>b.onclick=()=>{document.querySelectorAll(".admin-nav button").forEach(x=>x.classList.remove("active"));b.classList.add("active");document.querySelectorAll(".admin-panel").forEach(x=>x.classList.remove("active"));document.getElementById("panel-"+b.dataset.panel).classList.add("active")});
function rows(){try{return JSON.parse(localStorage.getItem("poa_requests")||"[]")}catch(e){return[]}}
function renderAppts(){let r=rows();document.getElementById("apptRows").innerHTML=r.map(x=>`<tr><td><span class="status ${x.status}">${x.status}</span></td><td>${x.client}<br><small>${x.phone||""}</small></td><td>${x.artist}</td><td>${x.service}</td><td>${x.date}<br>${x.time}</td><td>$${x.deposit_due||0}</td><td>${x.total==null?"Custom / TBD":"$"+x.total}</td><td>${x.status==="pending"?`<button class="btn" data-id="${x.id}" data-status="accepted">Accept</button> <button class="btn" data-id="${x.id}" data-status="declined">Decline</button>`:"—"}</td></tr>`).join("")||'<tr><td colspan="8">No demo requests yet.</td></tr>';document.querySelectorAll("[data-status]").forEach(b=>b.onclick=()=>{let all=rows(),x=all.find(q=>q.id===b.dataset.id);if(x)x.status=b.dataset.status;localStorage.setItem("poa_requests",JSON.stringify(all));renderAppts()})}
document.querySelectorAll(".cms-demo").forEach(b=>b.onclick=()=>alert("The no-code editor interface is ready. To make Publish save across all devices and update the public website, connect the included Supabase backend. No HTML editing will be needed after that."));
